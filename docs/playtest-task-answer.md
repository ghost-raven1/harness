# Ответ модели и продолжение переписки

Исправление воспроизводит экран задачи, на котором модель задала уточняющий вопрос, а CLI показал «Готово» и сократил ответ до одной строки.

## Поведение

- `completed` сохраняет прежний контракт завершения хода; пользовательская подпись — «Ответ получен». Завершение ответа не означает подтверждённое выполнение всей задачи. Текст не классифицируется по вопросительным знакам или другим эвристикам.
- «Ответить или продолжить» передаёт следующий вопрос в ту же сессию, с прежней папкой, профилем, исходной задачей, вопросом модели и результатами инструментов.
- Карточка сохраняет переносы и абзацы короткого ответа. Длинный ответ сопровождается ссылкой на пункт «Прочитать ответ». Уведомления о сохранении и ошибках имеют собственное место.
- Полный ответ открывается с начала в отдельном живом экране. Поддерживаются прокрутка, изменение размера терминала и возврат. Удаление из другого окна убирает старый текст и возвращает к каталогу; ошибка соединения остаётся ошибкой соединения.
- Меню и страницы чтения ограничены 108 колонками. Исходный Markdown, команды, пути и сохранённые ответы не переписываются.

## Терминальная проверка

На macOS arm64, Node 24.21.0, прошли требования зависимостей, форматирование, typecheck, сборка и полный Vitest: **353 теста в 49 файлах**. Первый полный проход обнаружил одно устаревшее ожидание списка действий скрытой задачи; оно обновлено для нового пункта чтения, после чего полный набор повторно прошёл. Исходники приложения между двумя проходами не менялись.

Настоящий PTY прошёл **30 проверок в 36 кадрах**: окна **48×24, 80×24 и 200×24**, а также изменение размера открытого экрана. В локальном тестовом провайдере проверены вопрос из пользовательского примера, ответ человека и содержимое следующего model-запроса. Подтверждены сохранение сессии, папки и профиля, чтение длинного ответа с начала/до конца, повторное открытие, перезапуск и удаление другим клиентом.

На той же сборке повторены **18 основных сценариев CLI** с 6 запросами к локальному HTTP-провайдеру. Код завершения — 0. Полный Linux-набор в этой доработке интерфейса повторно не запускался; его прежние результаты относятся к сборке, указанной в предыдущем отчёте.

Источник приложения: `93f40d3b530ccaa1e3aa69d2137dc0b234c8f2f68fe971526f355df89806396d`; JavaScript: `8de8d80936550325ac40c750af97fa5e4d39cf121e6495875680185cde05c532`. Сборка во время PTY не менялась. Структурированные результаты: [playtest-answer-before-coordination.json](history/playtest-answer-before-coordination.json), кадры: `.harness/answer-frames.json`.

Воспроизведение: `python scripts/playtest-answer.py --node /полный/путь/к/node-24.21.0`, требуется `pyte==0.8.2`. Тесты работают с временными данными. Пользовательская задача прочитана для диагностики, её продолжение не запускалось. Облачные вызовы, нативные Windows/Linux-терминалы в этот проход не входят.

Подготовка запуска прошла; повторная подготовка использовала кеш. SHA JavaScript совпал с проверенной сборкой. Обновлённый CLI открыт в Terminal. Снимок пользовательской задачи остался побайтно прежним. Прежние live/readability-отчёты сохранены как исторические; их сценарии целиком в этом проходе не повторялись.
